import 'package:flutter/material.dart';

class AlienSpecificationDesktop extends StatelessWidget {
  const AlienSpecificationDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
