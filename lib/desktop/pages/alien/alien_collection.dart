import 'package:flutter/material.dart';

class AlienCollectionDesktop extends StatelessWidget {
  const AlienCollectionDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
