import 'package:flutter/material.dart';

class LittleMiceSpecificationDesktop extends StatelessWidget {
  const LittleMiceSpecificationDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
