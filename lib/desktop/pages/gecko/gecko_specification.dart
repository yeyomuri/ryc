import 'package:flutter/material.dart';

class GeckoSpecificationDesktop extends StatelessWidget {
  const GeckoSpecificationDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
